import { SignInButton } from "~/app/(auth-page)/sign-in/SignInButton";

const Page = () => {
  return (
    <div className="h-screen flex justify-center items-center">
      <SignInButton />
    </div>
  );
};

export default Page;
